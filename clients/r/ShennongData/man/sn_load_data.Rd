% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lazy.R
\name{sn_load_data}
\alias{sn_load_data}
\title{Create a lazy Shennong dataset handle}
\usage{
sn_load_data(
  dataset,
  version = "latest",
  api_url = sn_get_api_url(),
  token = sn_get_api_token(),
  assay = "rna",
  data_model = NULL,
  layer = NULL,
  measure = "expression",
  limit = 1000
)
}
\arguments{
\item{dataset}{Dataset id in the Shennong catalog.}

\item{version}{Dataset version. \code{"latest"} resolves on the server.}

\item{api_url}{Shennong Data Server base URL.}

\item{token}{Optional bearer token.}

\item{assay}{Assay name, usually \code{"rna"}.}

\item{data_model}{Optional semantic data model, for example \code{"bulk"} or
\code{"single_cell"}. When \code{NULL}, the server infers the model from the catalog.}

\item{layer}{Optional layer name such as \code{"log2_tpm"} or \code{"lognorm"}.}

\item{measure}{Query measure, usually \code{"expression"}.}

\item{limit}{Default page size for later collection.}
}
\value{
A lazy Shennong dataset object.
}
\description{
\code{sn_load_data()} does not download matrix or observation data. It only stores
the dataset id, server URL, and query defaults. Use \code{filter()} and \code{select()}
to refine the handle, then call \code{sn_collect()}, \code{sn_fetch_genes()}, or a plot
helper to request a bounded page from \verb{/v1/query}.
}
