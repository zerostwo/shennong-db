# WebUI visual QA

The baseline screenshots are generated by `webui/playwright/visual.spec.ts` at
the desktop and mobile reference viewports.

The `*-live-1512x801.png` captures exercise the deployed Agent, Search,
provider settings, and Resources flows at the supplied desktop reference size.
The `design-qa-*-comparison-1512x801.png` files place the supplied ChatGPT
references and ShennongDB implementation in one image for the blocking review
record in [`design-qa.md`](../../../design-qa.md).
